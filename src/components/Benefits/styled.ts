import { Stack } from '@mui/material'
import { styled as styledMui } from '@mui/material/styles'

export const StyledCardGroup = styledMui(Stack)`
    div:nth-child(3) {
        margin-top: 3.5rem;
    }
`